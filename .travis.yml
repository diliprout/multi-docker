sudo: required
services:
  - docker

before_install:
  - docker build -t drout/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true drout/react-test npm test 

after_success:
  - docker build -t drout/multi-client ./client
  - docker build -t drout/multi-nginx ./nginx
  - docker build -t drout/multi-server ./server
  - docker build -t drout/multi-worker ./worker

  #Log in to Docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  
  #Take images from above and push them to Docker Hub
  - docker push dr98docker/drepo98:drout/multi-client
  - docker push dr98docker/drepo98:drout/multi-nginx
  - docker push dr98docker/drepo98:drout/multi-server
  - docker push dr98docker/drepo98:drout/multi-worker

  # deploy:
  #   provider: elasticbeanstalk
  #   region: <sub-domain present in aws url>
  #   app: multi-docker
  #   env: MultiDocker-env
  #   bucket_name: <search s3 and then your sub-domain, then open the sub-domain>
  #   bucket_path: docker-multi
  #   on:
  #     branch: master
  #   access_key_id: $AWS_ACCESS_KEY
  #   secret_access_key:
  #     secure: $AWS_SECRET_KEY
